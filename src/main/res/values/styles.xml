<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
    </style>
    <!--登录编辑框-->
    <style name="LoginEditView" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/edit_background_</item>
        <item name="android:singleLine">true</item>
        <item name="android:maxLength">20</item>
    </style>
    <!--订单列表-->
    android:textColor="#FFFA0909"
    android:textSize="15sp"

    <style name="OrderLit">
        <item name="android:textColor">#FFFA0909</item>
        <item name="android:textSize">15sp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>
    <!--登录编辑框-->
    <style name="LoginSpecial" parent="@android:style/Widget.EditText">

        <item name="android:background">@drawable/edit_background_</item>
        <item name="android:singleLine">true</item>
        <item name="android:maxLength">50</item>
    </style>
    <style name="MyPopupMenu" >
        <item name="android:popupBackground">#50f500</item>
    </style>
    <style name="CustomProgressDialog" parent="@style/CustomDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <!-- activity不变暗 -->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
    </style>

    <style name="MyDialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!-- //对话框无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- //此对话框的背景 -->
        <!-- 设置背景迁安透明 @color/transparent -->
        <item name="android:windowIsFloating">true</item>
        <!-- //对话框是否浮动 -->
        <item name="android:windowContentOverlay">@null</item>
        <!-- //对话框是否有遮盖 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    -

    <style name="AnimationPreview">
        <item name="android:windowEnterAnimation">@anim/pop_push_up_in</item>
        <item name="android:windowExitAnimation">@anim/pop_push_down_out</item>
    </style>

    <style name="GD_Theme" parent="android:style/Theme.Holo.Light.NoActionBar"></style>
</resources>
